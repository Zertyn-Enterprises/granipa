# Pass 9 — Estados vacíos/error

Pass 8 cerrado (menú Processing notes…, toast warning 2.4s, elapsed h:mm:ss). Tema: empty/error accionables. No rehacer onboarding (pass 10).

## Kimi
- Home `emptyState`: CTA Record / Quick note debajo del copy (el header ya los tiene; el vacío no).
- HUD + RecordingBar: si `transcription.phase` es `.failed`, botón Retry → `app.transcription?.retryIfFailed()`. Color `Theme.statusFailed` (no `.red`/`.orange`).
- Transcript vacío en detalle: si hay live `.failed`, el error + Retry, no un ScrollView vacío.
- `PermissionsListView`: si estado `.notDetermined`, botón Request (mic/calendar/notifications). Denied ya tiene Open Settings.
- `MainWindow` loadError: si el texto huele a permiso, botón Open Settings además de OK.

## GLM
- `TranscriptionCoordinator.retryIfFailed()`: solo si `phase` es `.failed`; re-intenta `ensureInstalled` + canales. No dupliques streams si no falló. Tests si el método es fácil de cubrir; si no, build basta.
- Cero UI.

## Grok
- `PermissionCenter`: `requestMicrophone` / `requestCalendar` / `requestNotifications` (luego `refresh()`).
- Overlay dictado: en `.failed` con preview vacío, copy "Press the shortcut again to retry."
- README: transcripción se puede reintentar si el modelo falla al preparar.

`swift build`. loop-log. landed:. No commits.
