# Pass 10 — Onboarding

Pass 9 cerrado (empty/error accionables, retryIfFailed, Request en permisos). Tema: 3 pasos, atajos reales, cloud opt-in. No pases a Home/sidebar (pass 11).

## Kimi
- Siguen 3 pasos. No añadas un cuarto.
- Welcome + fila de atajos: el hold de dictado usa `DictationController.shortcutLabel` (no hardcodees "Right Option").
- Paso 3 (AI & shortcuts): una línea clara — Muse y rewrite están **off** hasta Settings → Dictation. No enciendas cloud desde el tour.
- Checkmarks de CLI: `Theme.statusDone`, no `.green`.
- Si el paso 3 clippea a 600 pt, aprieta spacing. No subas el frame a lo loco.

## GLM
- Cero UI. Confirma defaults: `meetingSystemEngine` no es muse salvo opt-in; dictation engine `local`; rewrite `off`. `usesMuseForSystem` ya exige key. Si un default está ON, corrígelo. Tests intactos si no hay bug.

## Grok
- `DictationController.shortcutLabel` desde keyCode/modifiers reales.
- README: tour de 3 pasos; cloud off hasta Settings.

`swift build`. loop-log. landed:. No commits.
