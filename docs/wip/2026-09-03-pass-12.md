# Pass 12 — Meeting detail

Pass 11 cerrado (empty de carpeta, statusListening, folder anima, search se limpia). Tema: chrome, tabs, **no salto forzado a Enhanced**. No toques Home/sidebar.

## Kimi
- Quita `tab = .enhanced` en `onAppear` / `onChange(enhancingMeetingIDs)`. El usuario se queda en Notes/Transcript.
- Si ese meeting se está enhancing: badge/dot en el tab Enhanced (no cambies de tab).
- Underline de tabs: un indicador (p.ej. `matchedGeometryEffect`), gated Reduce Motion — no tres rectángulos independientes.
- Chrome: chip de la carpeta actual en el header si hay folder (el ⋯ ya mueve; esto solo muestra).

## GLM
- Cero UI. Grep `enhanced` / `tab` en Transcription/Audio. 0 hits esperados. No toques código.

## Grok
- README: al terminar una grabación te quedas en Notes; Enhanced muestra progreso si entras.

`swift build`. loop-log. landed:. No commits.
