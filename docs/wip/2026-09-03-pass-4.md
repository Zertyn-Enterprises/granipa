# Pass 4 — Captions + HUD

Pass 3 cerrado (dictado: mic al pulsar, fade 0.08 s). Tema: captions y HUD, sin clip, dismiss claro.

## Kimi
- Captions: `relayout` que no recorte 2+2 líneas; Hide/Show en menu bar ya existe, que funcione.
- HUD: no cerrar al Stop si el meeting pasa a `processing` — el usuario no ve un agujero. Cerrar cuando `status == .ready` o failed.
- `LiveTranscriptSnippet` es la única superficie de texto live (HUD + captions).

## GLM
- `systemWarning` tiene que llegar a UI (HUD ya lo pinta). No toques UI.
- Caps: adopt merge no debe hacer desaparecer Them un frame. Si ya está, no retocar.

## Grok
- Menu bar ya tiene elapsed + Show HUD. No pises MainWindow.
- README: una línea de que captions se ocultan desde el menú.

`swift build`. loop-log. landed:. No commits.
