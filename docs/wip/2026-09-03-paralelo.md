# Trabajo en paralelo — los 3 escriben código

Checkout compartido `feat/dictation-overlay`. **Sin worktrees.** Para no pisaros: dueños de ficheros. Si necesitas un fichero que no es tuyo: nota en `docs/wip/` y `landed:`, no lo edites.

No commits a `main`. No `git commit` a menos que Grok lo pida (un working tree, un historial).

## Dueños (round 3 implementación)

| Quién | Ficheros | Qué implementar YA |
|---|---|---|
| **Kimi** | `UI/SettingsView.swift`, `UI/Theme.swift`, `UI/MainWindow.swift`, `UI/HomeView.swift`, `UI/SidebarView.swift`, `UI/MeetingDetailView.swift`, `UI/OnboardingView.swift`, `UI/MenuBarView.swift`, `UI/RecordingHUD.swift`, `UI/RecordingBar.swift`, `UI/CaptionsOverlay*.swift`, `Clipboard/ToastController.swift` | Tus P1-1 tabs, P1-2 extraer status/snippet, P1-3 motion (gated Reduce Motion), P2-1 spaces vacío, P2-2 chrome detalle, P2-3 toast, P2-4 copy 20 MB |
| **GLM** | `Transcription/TranscriptionCoordinator.swift`, `Dictation/MuseSystemTranscriber.swift`, `Dictation/MuseDictationEngine.swift`, `Transcription/MeetingASRPolicy.swift`, `Transcription/LanguageDetection.swift` (si hace falta el voto Muse), tests de eso | Tu P0-1 (no re-consumir stream), P1-1 warning, P1-2 reconnect ~55 min, P1-3 locale+Muse, P2-1 adopt merge |
| **Grok** | `AppState.swift`, `GranipaApp.swift`, `Dictation/DictationController.swift`, `LLM/RewriteClient.swift`, `README.md`, `CLAUDE.md`, orquestación | GLM P2-2 flag after finishAndWait; GLM P2-3 rewrite error+toast; Kimi P1-4 menu bar elapsed/processing |

Si chocáis: parar, `landed: blocked file X`, Grok resuelve.

Al terminar: una línea en `docs/wip/2026-09-03-round3-applied.md` (append) con lo que tocaste. `landed:` al otro.
