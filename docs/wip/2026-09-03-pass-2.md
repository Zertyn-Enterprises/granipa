# Pass 2 — Idle / recursos

Pass 1 cerrado (96 tests, round 3 aterrizado). Tema: nada despierto de más con la app abierta y sin grabar.

Dueños iguales (`paralelo.md`). Extra este pass: GLM puede tocar `Calendar/CalendarService.swift`, `Clipboard/ClipboardMonitor.swift`, `Detection/MeetingDetector.swift`. Kimi no. Grok no.

## Kimi
- TimelineView / animaciones solo con grabación o dictado visible. Nada palpitando en idle.
- HUD ya pinta `transcription?.systemWarning` — no lo quites.
- ClipboardHistoryView: toast de Accessibility en `.warning` (ese fichero no tenía dueño; tómalo).

## GLM
- ClipboardMonitor: 700 ms → 1 s cuando está on (pasteboard no tiene eventos).
- MeetingDetector: no hacer CoreAudio sync cada 5 s si puedes listener de process list; si no, dejar 5 s pero documentar.
- CalendarService: 300 s está bien; no refrescar si `access != granted`.
- No reabrir Muse en idle.

## Grok
- AppState: toast "couldn't format fully" → `.warning`
- DictationController: "grant Accessibility" → `.warning`

`swift build`. Append `loop-log.md`. `landed:` al otro. No commits. No `main`.
