# Round 3 applied (append-only)

- 2026-09-03 Grok: AppState lee `systemUsedMuse` DESPUÉS de `finishAndWait`. RewriteError propio + toast + timeout 5s. Menu bar elapsed + icono processing. README 20 MB. Dueños en `paralelo.md`.
- 2026-09-03 Kimi: Settings 9→6 tabs (Templates→AI, API+Webhooks→Integrations, Productivity+Windows→Extras, segmented sub-pickers). Nuevo `UI/RecordingSharedViews.swift` (RecordingTimer, AudioWarningLabel, LiveTranscriptSnippet) adoptado por HUD/Bar/Captions. Motion: banner detección slide+fade, tabs detalle, filas Home — todo gated Reduce Motion. Sidebar sin "SPACES" vacío. Detalle: folder+template dentro del ⋯. Toast: tokens Theme + estilo semántico success/warning + fade (top-centro a propósito: abajo colisiona con captions/dictado). Copy diarización ~20 MB (Settings). Pendiente para dueños: toast `.warning` en AppState:408 ("couldn't format fully", Grok) y DictationController "grant Accessibility" (Grok); callers en ClipboardHistoryView sin tocar (no es mi fichero).
- 2026-09-03 GLM: `MuseSystemTranscriber` reescrito como sesión resiliente (actor): split pre/post-consumo — los fallos de transporte post-primer-chunk NUNCA throw (re-consumir el AsyncStream es imposible), reconexión interna con presupuesto (5), rotación proactiva a los 55 min (límite Muse 60), descarte de backlog >4 s (send-ahead). Fallos pre-consumo (connect/handshake/audioFormat) siguen propagando → el fallback Apple del coordinator recibe un stream VIRGEN (P0-1). `TranscriptionCoordinator`: `systemWarning` observable + `onStatus` (P1-1 — NOTA para Kimi: renderizar `app.transcription?.systemWarning` en el nuevo `AudioWarningLabel` compartido); `applySystem` acumula finals de Muse como `systemHint` para la decisión de idioma (P1-3); `adopt` fusiona mic finals con system finals ya visibles vía `mergedLiveSegments` (P2-1); fallback cubre también `.audioFormat`. `LanguageDetection.decide(systemHint:)`: el texto del sistema vota al candidato que lee, solo si ese candidato ya tiene texto de micro (adopt es irreversible). Tests: 2 de merge + 2 de hint (96 tests, 19 suites, verdes — build limpio cuando ToastController cerró). No toqué UI/, AppState ni DictationController.
