# Pass 8 — Menu bar + toasts

Pass 7 cerrado (tokens Theme; overlay dictado fillSubtle/strokeStrong). Tema: menú de estado y toasts, no rehacer Reduce Motion ni paleta.

## Kimi
- `MenuBarView`: fila deshabilitada "Processing notes…" si algún meeting está `.processing`. No toques `GranipaApp`/`MenuBarLabel`.
- `ToastController`: dwell de `.warning` más largo que success (p.ej. 2.4 s vs 1.8 s). Reduce Motion ya está — no lo rompas.
- No relances hex/motion.

## GLM
- Cero UI. Grep `ToastController` en Transcription/, Audio/, Muse*. Si hay llamada en el hot path, quítala o muévela al coordinador MainActor. Si 0 hits, no toques código.
- Tests intactos.

## Grok
- `MenuBarLabel`: elapsed `h:mm:ss` a partir de 60 min.
- README: menú muestra elapsed al grabar y glifo al procesar.
- `OCRService`: "No text found" → `.warning`.

`swift build`. loop-log. landed:. No commits.
