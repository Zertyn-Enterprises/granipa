# V2 build evidence — 2026-09-05

## Delivery policy

The user replaced per-patch cross-review with one review of a complete integrated
screen or large feature. Implementers compile and run relevant tests during work.
Codex orchestrates, integrates and verifies; Grok/GLM implement. Kimi's official
service reported weekly quota exhaustion earlier in this run.

## Integrated at 1945563

- Existing V2 shell, sidebar destinations and adaptable dictation inspector.
- Debug fixture and isolated runner (40ba05d, ef15797).
- Native menu redesign (1945563): capture/tools/app, nested battery controls,
  Settings, truthful processing status and accessible status icon.
- Integrated suite: 243 tests in 42 suites passed.
- Signed bundle: `build/Grañipa.app`, generated by `Scripts/bundle.sh`.
- Original shell bundle signature verified with `codesign --verify --deep --strict`;
  TeamIdentifier R4V252C833. Subsequent menu bundle script also completed verification.

## Runtime observations

- Closed the installed app normally with its application quit event.
- Shell build process 61196 started from this worktree and remained alive.
- A 60-second `top -pid 61196 -l 13 -s 5 -stats pid,cpu,rsize` run recorded
  0.1% CPU in all twelve post-initial samples; rsize was approximately 40M.
  This is one idle observation, not an A/B performance claim or a recording profile.
- Menu build fixture process 50530 stayed alive past one minute. `lsof` showed its
  SQLite file only under `/private/tmp/granipa-v2-fixture.9ilDFD/` in the inspected
  database paths. `lsof -nP -a -p 50530 -i` returned no network connections at that sample.
  Sparkle's linked framework was mapped; absence of network at one sample does not
  establish lifetime absence of activity.
- Fixture closed normally; runner exited 0 and its exact temporary directory was removed.

## Still unverified

- Visual comparison and keyboard/VoiceOver interaction: Apple Events inspection of
  System Events returned -1743; screencapture returned `could not create image from display`.
- Record/dictation/clipboard end-to-end with the V2 build; no speech was simulated.
- Final screen review, Dictation and Permissions implementations are in progress.
- V2 as a whole is not complete. External providers/account access still need a user choice.

The prior goal remains blocked in the goal service. `create_goal` rejected a new goal
because the thread has an unfinished goal. No goal was falsely marked complete.
