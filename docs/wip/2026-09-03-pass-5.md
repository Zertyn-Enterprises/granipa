# Pass 5 — Settings

Pass 4 cerrado (HUD processing, captions). Tabs ya son 6. Tema: copy honesto, defaults, key Muse.

## Kimi
- Settings: ventana no clippea Dictation/rewrite. Copy Muse = solo audio del ordenador en reuniones.
- Extras: clipboard/windows siguen pudiéndose apagar; no escondas capacidad.
- Cero hex sueltos en Settings (tokens Theme).

## GLM
- `MeetingASRPolicy.usesMuseForSystem`: usar `KeychainStore.hasMuseKey()` (Grok lo añade). Invalidar al guardar la key ya lo hace `set`.
- No toques UI/.

## Grok
- `KeychainStore.hasMuseKey()` cacheado. README: lista Settings 6 tabs.

`swift build`. loop-log. landed:. No commits.
