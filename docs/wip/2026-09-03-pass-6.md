# Pass 6 — Motion

Pass 5 cerrado (Settings 6 tabs, hasMuseKey). Tema: un sistema de motion, Reduce Motion en todo.

## Kimi
- `Theme.motionFast` / `motionNormal` (0.08 / 0.15) y usarlos en hover, Home, MainWindow, LevelMeter, ActivityDot.
- HoverHighlight y `.animation(.linear(duration: 0.1), value: level)` gated Reduce Motion.
- Overlay ya tiene 0.08 + Reduce Motion — no lo rompas.

## GLM
- Cero animaciones en el hot path de audio. No toques UI.

## Grok
- README: Reduce Motion. Overlay AppKit ya gated.

`swift build`. loop-log. landed:. No commits.
