# Pass 11 — Home + sidebar

Pass 10 cerrado (onboarding 3 pasos, shortcutLabel, cloud off). Tema: densidad, spaces vacíos, transiciones. No toques MeetingDetail (pass 12).

## Kimi
- Home empty si hay `activeFolder`: "No meetings in this folder", no "No meetings yet".
- `HomeMeetingRow` Recording `.red` → `Theme.statusListening`.
- `MainWindow`: `Task { await openSettings() }` → `openSettings()` (warning: await sin async).
- Animar también el cambio de carpeta (`selectedFolderID`), gated Reduce Motion.
- Sidebar: al elegir una carpeta, limpia `searchQuery` (Home ya lo hace).
- SPACES vacío ya está oculto — no lo rompas.

## GLM
- Cero UI. Grep Home/Sidebar en Transcription/Audio: 0 hits esperados. No toques código.

## Grok
- `createFolder`: selecciona la carpeta nueva (`selectedFolderID`, `selectedMeetingID = nil`).
- README: Spaces en el sidebar solo cuando hay carpetas.

`swift build`. loop-log. landed:. No commits.
