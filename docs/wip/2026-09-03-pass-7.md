# Pass 7 — Tokens

Pass 6 cerrado (Theme.motion*, Reduce Motion en hover/HUD/toast/overlay/MainWindow). Tema: cero hex sueltos; fills/strokes/radii repetidos viven en Theme.

## Kimi
- `Theme.channelMe = Color(hex: 0x6FA8DC)` y usarlo en `MeetingDetailView` SegmentRow y `RecordingSharedViews` (los dos hex sueltos).
- `Theme.fillSubtle` (white 0.08), `fillHover` (0.05), `strokeStrong` (0.12). Sustituir `Color.white.opacity(0.0x)` repetidos en UI/ + ClipboardHistoryView + HoverHighlight/Avatar.
- `.fill(.green)` en ActivityDot y LevelMeter → `Theme.statusDone`. Sidebar rec dot `.fill(.red)` → `Theme.statusListening`.
- No inventes una escala enorme de spacing. Solo valores que ya se copian.

## GLM
- Cero UI. Grep `Color(hex:` / `.fill(.green)` en Transcription/, Audio/, Dictation/Muse*, MeetingASRPolicy. Si no hay, no toques código.
- Tests intactos.

## Grok
- Overlay dictado adopta `fillSubtle` / `strokeStrong` cuando existan.
- README: la paleta vive en Theme.
- No edites Theme.swift.

`swift build`. loop-log. landed:. No commits.
