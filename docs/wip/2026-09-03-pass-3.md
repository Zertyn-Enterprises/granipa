# Pass 3 — Dictado instantáneo

Pass 2 cerrado (idle). Tema: hold → overlay → primera palabra, percibido <300 ms.

## Kimi
- Overlay: fade de entrada ≤0.08 s (ahora 0.15). Visible en el key-down, no después del modelo.
- Copy "Preparing" vs "Listening" claro. Reduce Motion: sin fade, `alphaValue = 1` inmediato.

## GLM
- `ChannelTranscriber` / SpeechAnalyzer: `prepareToAnalyze` no debe retrasar el primer volatile. Si hay doble `ensureInstalled` (controller + AppleDictationEngine), deja uno.
- No toques DictationController (Grok).

## Grok
- Mic ON al pulsar, en paralelo al prepare del modelo; el stream acumula audio mientras descarga.
- `ensureInstalled` una sola vez.

`swift build`. Append loop-log. `landed:`. No commits.
