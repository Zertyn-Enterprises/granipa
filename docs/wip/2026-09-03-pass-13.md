# Pass 13 — Tests

Pass 12 cerrado (sin salto a Enhanced, badge, underline único, chip carpeta). Tema: tests de contrato. No dupliques los que ya están (PCM, Muse parser, rewrite parse básico, MeetingASRPolicy key, LanguageDetection+hint, ChunkFan).

## Kimi
- Nuevo `Tests/GranipaTests/ThemeTests.swift`: `motionFast` 0.08, `motionNormal` 0.15, tokens `channelMe`/`fillSubtle`/`strokeStrong` existen (compilan). Cero tests de montaje de vistas.
- No edites `DictationTests.swift`.

## GLM
- Nuevo `Tests/GranipaTests/MeetingASRPolicyTests.swift`: `engine` nil, `""`, `"muse"` sin key, `"local"` con key → false; solo muse+key → true.
- Si hay un hueco real en Muse parser o LanguageDetection no cubierto, un test. No dupliques `systemHintVoteBreaksForceTie`.
- No edites `DictationTests.swift`.

## Grok
- Nuevo `RewriteClientTests.swift`: URL vacía/trailing slash; parse content vacío/whitespace/JSON roto; `RewriteError` descriptions.
- Nuevo `DictationShortcutTests.swift`: `shortcutLabel` default / Right ⌘ / ⌥ Space (restaura UserDefaults).
- `swift test`.

loop-log. landed:. No commits.
