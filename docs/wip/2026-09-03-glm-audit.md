# GLM — auditoría adversarial arquitectura / recursos / ASR (fase 1, SOLO LECTURA)

Lee primero `docs/wip/2026-09-03-orquestacion.md`. Eres GLM. No escribas código. No commits. No ramas.

Cuando termines, escribe `docs/wip/2026-09-03-glm-hallazgos.md` y manda a Kimi (ListAgents → `granipa-kimi` / uuid `e36502c8-64a8-48cc-a4d5-5dae5f813fd3`) un único `landed: arch/perf/ASR audit in docs/wip/2026-09-03-glm-hallazgos.md`.

Si Kimi ya dejó `docs/wip/2026-09-03-kimi-hallazgos.md`, léelo y escribe la réplica en `docs/wip/2026-09-03-glm-replica.md`. Si no existe, espera ~15 min, relee, replica. Si no aparece: `blocked: Kimi hallazgos ausentes` al final de tu informe.

## Objetivo

Atacar arquitectura, recursos del Mac, y la vía de transcripción local OSS. El humano quiere live transcription tipo SuperWhisper **y** mínimos recursos. Esas dos tensan. Tu trabajo es el tradeoff con evidencia, no un yes-and de features.

## Scope (leer)

- `Sources/Granipa/AppState.swift` (orquestador 536 líneas)
- `Sources/Granipa/Audio/` (tap, mic, session, engine)
- `Sources/Granipa/Transcription/` (coordinator, channel, probes, models)
- `Sources/Granipa/Diarization/`
- `Sources/Granipa/Detection/MeetingDetector.swift`
- `Sources/Granipa/Clipboard/ClipboardMonitor.swift`
- `Sources/Granipa/API/` (server siempre-on)
- `Sources/Granipa/Calendar/CalendarService.swift`
- `Sources/Granipa/LLM/` (subprocess CLIs)
- `Sources/Granipa/Storage/AppDatabase.swift` (migraciones)
- `Package.swift` vs `CLAUDE.md` vs README
- Tests: qué cubren vs blast radius (auth API, persistencia, audio)

FluidAudio: **verificar la API del pin `0.15.2`**, no la de `main`. El pin está en `Package.swift`. Mira el checkout en `.build` / `Package.resolved` y el código importado (`OfflineDiarizerManager`). Streaming ASR puede no existir en 0.15.2.

SuperWhisper (Grok ya leyó; no trates como misterio):
- Realtime = solo Nova cloud.
- Local = whisper.cpp (Nano 150 MB … Ultra 3 GB) + Parakeet via WhisperKit.
- Patrón clave: unload del modelo a los N segundos (10s–1h) para no dejar RAM caliente.

Docs FluidAudio actuales (pueden ser más nuevas que 0.15.2): https://docs.fluidinference.com/introduction — Parakeet EOU 120M streaming, TDT 0.6B batch, ANE.

## Preguntas que DEBES responder con evidencia

1. **Siempre-on.** Lista cada loop/servicio que corre con la app idle. Intervalo, hilo, I/O. Clipboard 700 ms, detector 5 s, webhooks 30 s, API :7799 — confirma en código. ¿Default on es correcto?
2. **Grabación.** SystemAudioTap + MicRecorder + dos writers m4a + silence padding + fan-out de chunks. ¿Doble transcripción de dos canales es el mínimo? ¿AEC voice-processing cuesta CPU? ¿Device-change recovery (commit a1e4466) deja algo vivo?
3. **SpeechAnalyzer.** `ChannelTranscriber` + probes paralelos. Coste de 1 vs 6 analyzers. `SpeechModels.ensureInstalled` en el hot path. ¿Se puede un solo analyzer y cambiar locale?
4. **FluidAudio hoy.** Solo diarization post-hoc. `prepareModels()` cada meeting. ¿Se descarga ~130 MB cada vez o hay cache? ¿Se descarga en idle? CLAUDE.md vs Package.swift.
5. **Candidatos ASR local (comparar, no elegir por el humano):**
   - A. Seguir con SpeechAnalyzer (ya live, Apple, ANE, 15+ langs, 0 deps nuevas).
   - B. FluidAudio Parakeet EOU/TDT (dep ya en el grafo; bump de versión probable; EOU en-only; TDT 25 langs batch).
   - C. whisper.cpp / WhisperKit (como SuperWhisper; RAM 75 MB–3 GB; no es streaming de verdad).
   - D. Híbrido: SpeechAnalyzer live + Parakeet/Whisper rescore al parar.
   Por cada uno: RAM idle, RAM recording, ANE vs CPU/GPU, latencia percibida, langs, trabajo de integración, riesgo de bundle.
6. **AppState god object.** Qué extraer vs qué dejar. No proponer Clean Architecture.
7. **Simplificar.** Código muerto, `#if canImport` ya inútil, features que no pagan su coste (API server default on, window manager, clipboard). Cada recorte: líneas, CPU, complejidad.
8. **Tests.** 14 ficheros. ¿Hay test del coordinator de transcripción? ¿Del recording session? Huecos 🔴 (API token, DB migrations, webhook HMAC).
9. **Energía.** App idle ~99 MB RSS / 0% CPU (pid 28248, un sample). El dolor es en recording+transcribe. Instrumenta con lectura de código: timers, polling vs event, `TimelineView` 1s en HUD.
10. **Swift 6 / MainActor.** Capturas, hops innecesarios al main en audio.

## Formato del informe

```
# GLM hallazgos arch/perf/ASR — 2026-09-03
## Perfil de recursos (verificado)
## Hallazgos
### P0 …
- evidencia: path:línea
- coste ahora
- arreglo mínimo
## ASR: tabla A/B/C/D (hechos vs inferencia)
## Recortes (qué apagar / unload / fusionar)
## Réplica pendiente / hecha
```

Sé hostil. Si añadir Whisper local **empeora** el objetivo de mínimos recursos, dilo en P0. Si Grok se equivocó, corrígelo con archivo:línea. No implementes.
