# Pass 14 — Muse 55 min reconnect

Pass 13 cerrado (111 tests / 23 suites). Tema: reloj de reunión intacto al rotar; warning si Muse muere. No rehacer retryIfFailed ni ChunkFan.

## Kimi
- `CaptionsOverlayView`: si `systemWarning` no es nil, muéstralo (una línea, `Theme.statusFailed`). HUD ya lo pinta — no lo rompas.
- No toques MuseSystemTranscriber.

## GLM
- `rotate()`: si `connectFresh` falla, `markDead()` (hoy `try?` deja `dead=false` y `socket=nil` → rotate en cada chunk).
- `MeetingClock` de `private` a `internal` + tests: `advance`/`turnStart` no se resetean al “rotar”; `sessionRotateInterval == 55*60`.
- No toques UI.

## Grok
- README: Muse (canal Them) rota a los 55 min; timestamps siguen siendo meeting-relative; el HUD avisa si se pierde.

`swift build`. loop-log. landed:. No commits.
